name: My Deploy

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to production
        uses: johnbeynon/render-deploy-action@v0.0.8
        with:
          service-id: srv-chv21064dad0lp980emg?key=paklt1rEURg
          api-key: rnd_vYFIRsM4qiKOlpKt60I6se71OC0s
